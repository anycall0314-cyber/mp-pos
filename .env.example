# 把這支檔案複製成 .env 後填入真實值；.env 已加入 .gitignore

# Django
DJANGO_SETTINGS_MODULE=config.settings.dev
DJANGO_SECRET_KEY=change-me-in-real-env
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Database
DATABASE_URL=postgres://inventory:inventory@localhost:5432/inventory_3c

# CORS（前端 dev server）
CORS_ALLOWED_ORIGINS=http://localhost:5173

# Tenant（MVP 寫死 1，未來轉 SaaS 再改）
DEFAULT_TENANT_ID=1
