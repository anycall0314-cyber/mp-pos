# ─────────────────────────────────────────────────────────
# 前端建構時環境變數範本(Vite 編譯時 inline 進 JS bundle)
# ─────────────────────────────────────────────────────────
# 後端 API 的 base URL。
#  - 預設(留空 / 不設):走相對路徑 `/api/v1`,適用「前後端同 host」
#    (Django 直接 serve React build 的情境)
#  - 若前端拆出去單獨部署到 CDN(Vercel / Cloudflare Pages 等),改成絕對 URL,
#    例:VITE_API_BASE=https://api.mppos.example.com/api/v1
#
# 設好後跑 `npm run build` 把值 inline 進去
VITE_API_BASE=
